<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.juggist.jcore">

    <application
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity android:name=".BigViewPagerPhotoActivity" />
        <activity android:name=".view.chooseImg.ImageBucketActivity" />
        <activity android:name=".view.chooseImg.ImageGridActivity" />
        <activity android:name=".view.chooseImg.PhotoActivity" />
    </application>

</manifest>
