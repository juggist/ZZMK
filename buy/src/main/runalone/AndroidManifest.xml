<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.juggist.buy">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:name=".runalone.application.BuyApplication"
        android:allowBackup="true"
        android:label="home"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:replace="android:label">
        <activity android:name=".runalone.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.OrderSubmitActivity" />
        <activity android:name=".ui.DiscountCardChooseActivity" />
        <activity android:name=".ui.OrderPayActivity"/>
        <activity android:name="com.juggist.jcore.view.chooseImg.ImageBucketActivity" />
        <activity android:name="com.juggist.jcore.view.chooseImg.ImageGridActivity" />
        <activity android:name="com.juggist.jcore.view.chooseImg.PhotoActivity" />
    </application>

</manifest>
