<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"

    package="com.juggist.order">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <application
        android:allowBackup="true"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:label="order"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:replace="android:label">
        <activity android:name=".ui.OrderActivity" />
        <activity android:name=".ui.OrderRefundActivity" />
        <activity android:name=".ui.OrderTransportActivity" />
        <activity android:name=".ui.OrderRefundAllActivity" />
    </application>

</manifest>
